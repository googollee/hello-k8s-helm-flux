name: Build and push images to GHCR
on:
  push:
    branches:
      - main
    tags:
      - '*'
jobs:
  push-store-image:
    runs-on: ubuntu-latest
    steps:
      - name: 'Checkout GitHub action'
        uses: actions/checkout@main

      - name: 'Build image'
        run: |
          make image

      - name: 'Login to GHCR'
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: 'Push image, latest'
        if: ${{ github.ref.type != 'tag' }}
        run: |
          docker tag hello ghcr.io/googollee/hello-k8s-helm-flux:latest
          docker push ghcr.io/googollee/hello-k8s-helm-flux:latest

      - name: 'Push image, ${{ github.ref.name }}'
        if: ${{ github.ref.type == 'tag' }}
        run: |
          docker tag hello ghcr.io/googollee/hello-k8s-helm-flux:${{ github.ref.name }}
          docker push ghcr.io/googollee/hello-k8s-helm-flux:${{ github.ref.name }}
